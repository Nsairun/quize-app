import React from 'react';
import './Button.css';

function Button() {
  return (
    <div className="page-btn">
      <button type="button" onClick={[]}>
        Next
      </button>
    </div>
  );
}

export default Button;
